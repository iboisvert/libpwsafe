name: CMake workflow for libpwsafe

on: 
  push:
    branches: 
      - main
      - dev/*
    tags:
      - v*

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Setup environment
      run: |
        sudo apt-get -y update
        sudo apt-get -y install cmake gcc g++ doxygen graphviz libgtest-dev nettle-dev
    - uses: actions/checkout@v2
    - name: Configure
      run: cmake --preset=x64-linux-gcc-release
    - name: Build
      run: cmake --build --preset=x64-linux-gcc-release
    - name: Test
      run: ctest --preset=x64-linux-gcc-release
    - name: Package
      run: |
        cd build 
        cpack -G DEB
    - name: Push release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: build/*.deb
    #- name: make check
    #  run: make check
    #- name: make distcheck
    #  run: make distcheck
